version: 1
# Naive Bayes (MultinomialNB) hyperparameters
# Best parameters from alpha tuning

alpha: 0.05
fit_prior: true

# Alpha values used for hyperparameter tuning
tuning_alphas: [0.01, 0.05, 0.1, 0.5, 1.0, 2.0, 5.0]
